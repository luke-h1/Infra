---
- hosts: localhost
  become: yes
  vars:
    app_name: figma  
    app_url: /download/desktop/mac/
    installer_type: app
    install_method: dmg
    download_file_type: dmg
  roles:
    - setup
    - installer
    - cleanup
   